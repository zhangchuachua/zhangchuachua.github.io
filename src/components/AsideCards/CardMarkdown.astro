---
import Markdown from '../atomic/Markdown.astro'
import CardBase from './CardBase.astro'
interface Props {
  title: string,
  content: string
}
const { title, content } = Astro.props

---
<CardBase {title}>
  <Markdown render style="mini" set:html={content}/>
</CardBase>