---
import { makeBadge, type Format } from "badge-maker";

interface Props {
  format: Format;
}
const { format } = Astro.props;
const svg = makeBadge(format);
---

<span class="inline-block" set:html={svg} />
